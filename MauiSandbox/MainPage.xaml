<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MauiSandbox.Controls"
             xmlns:genericControls="clr-namespace:MauiSandbox.Controls.Generics"
             xmlns:viewModels="clr-namespace:MauiSandbox.ViewModels"
             xmlns:enums="clr-namespace:MauiSandbox.Enums"
             x:DataType="viewModels:MainViewModel"
             x:Class="MauiSandbox.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <controls:SingleOptionSelectorControl
                x:Name="singleOptionSelector"
                ItemSource="{Binding ItemSource}" />

            <!-- <genericControls:SingleOptionSelectorControl
                x:TypeArguments="enums:GAME_EQ_SLOT"
                ItemSource="{Binding GameEQPresetModeList}"
                PresetClickedCommand="{Binding PresetClickedCommand}" />

            <genericControls:SingleOptionSelectorControl
                x:TypeArguments="enums:MIC_EQ_SLOT"
                ItemSource="{Binding MicEQPresetModeList}"
                PresetClickedCommand="{Binding PresetClickedCommand}" /> -->

            <Button
                Text="Add option" 
                Command="{Binding AddOptionCommand}"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
